# Dev-mode CLI binary — points to the monorepo's CLI build output.
# The packaged .dmg bundles its own binary; this is only needed for local dev.
# Relative to apps/desktop/ — resolved by PiAgentBridge via path.resolve().
KATA_BIN_PATH=../../apps/cli/dist/loader.js
