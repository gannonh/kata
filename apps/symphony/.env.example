# Required
LINEAR_API_KEY=lin_api_...

# Codex auth — use ONE of these:
#   Option 1: run `codex auth` to log in via browser (ChatGPT/API subscription)
#   Option 2: set your API key here
# OPENAI_API_KEY=sk-...

# Log level (default: info). Use debug or trace for troubleshooting.
RUST_LOG=info

# Optional — for GitHub PR operations (clone, push, PR creation)
# GH_TOKEN=ghp_...
# GITHUB_TOKEN=ghp_...
