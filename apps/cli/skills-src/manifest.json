{
  "skills": [
    {
      "name": "kata-setup",
      "description": "Use when the user asks to install Kata, set up Kata CLI, connect Kata to an agent harness, or check initial Kata configuration.",
      "workflow": "setup",
      "runtimeRequired": false,
      "contractOperations": [
        "health.check"
      ],
      "setupHint": "Run `npx @kata-sh/cli setup` for generic harness detection, or `npx @kata-sh/cli setup --pi` for Pi harnesses, then verify with `npx @kata-sh/cli doctor`.",
      "legacyCommand": "apps/orchestrator-legacy/commands/kata/health.md",
      "legacyWorkflow": "apps/orchestrator-legacy/kata/workflows/health.md",
      "requiredReferences": [
        "cli-runtime",
        "artifact-contract"
      ],
      "requiredTemplates": []
    },
    {
      "name": "kata-new-project",
      "description": "Use when the user wants to start, define, import, or initialize a Kata project.",
      "workflow": "new-project",
      "contractOperations": [
        "project.upsert",
        "artifact.write",
        "health.check"
      ],
      "setupHint": "If Kata CLI is not configured, run `npx @kata-sh/cli setup`, then verify with `npx @kata-sh/cli doctor`.",
      "legacyCommand": "apps/orchestrator-legacy/commands/kata/new-project.md",
      "legacyWorkflow": "apps/orchestrator-legacy/kata/workflows/new-project.md",
      "requiredReferences": [
        "cli-runtime",
        "artifact-contract",
        "questioning",
        "ui-brand"
      ],
      "requiredTemplates": [
        "project",
        "requirements"
      ]
    },
    {
      "name": "kata-new-milestone",
      "description": "Use when the user wants to create the next milestone, release goal, or scoped delivery target for an existing Kata project.",
      "workflow": "new-milestone",
      "contractOperations": [
        "project.getContext",
        "milestone.create",
        "artifact.write"
      ],
      "setupHint": "If Kata CLI is not configured, run `npx @kata-sh/cli setup`, then verify with `npx @kata-sh/cli doctor`.",
      "legacyCommand": "apps/orchestrator-legacy/commands/kata/new-milestone.md",
      "legacyWorkflow": "apps/orchestrator-legacy/kata/workflows/new-milestone.md",
      "requiredReferences": [
        "cli-runtime",
        "artifact-contract",
        "questioning",
        "ui-brand"
      ],
      "requiredTemplates": [
        "project",
        "requirements",
        "roadmap",
        "state"
      ]
    },
    {
      "name": "kata-plan-phase",
      "description": "Use when the user asks to plan a phase, slice upcoming work, or turn a milestone goal into executable Kata tasks.",
      "workflow": "plan-phase",
      "contractOperations": [
        "project.getContext",
        "milestone.getActive",
        "slice.create",
        "task.create",
        "artifact.write"
      ],
      "setupHint": "If Kata CLI is not configured, run `npx @kata-sh/cli setup`, then verify with `npx @kata-sh/cli doctor`.",
      "legacyCommand": "apps/orchestrator-legacy/commands/kata/plan-phase.md",
      "legacyWorkflow": "apps/orchestrator-legacy/kata/workflows/plan-phase.md",
      "requiredReferences": [
        "cli-runtime",
        "artifact-contract",
        "ui-brand"
      ],
      "requiredTemplates": [
        "phase-prompt"
      ]
    },
    {
      "name": "kata-execute-phase",
      "description": "Use when the user wants Kata to execute planned phase work or advance tasks in the active milestone.",
      "workflow": "execute-phase",
      "contractOperations": [
        "project.getContext",
        "milestone.getActive",
        "slice.list",
        "task.list",
        "task.updateStatus",
        "artifact.read",
        "artifact.write"
      ],
      "setupHint": "If Kata CLI is not configured, run `npx @kata-sh/cli setup`, then verify with `npx @kata-sh/cli doctor`.",
      "legacyCommand": "apps/orchestrator-legacy/commands/kata/execute-phase.md",
      "legacyWorkflow": "apps/orchestrator-legacy/kata/workflows/execute-phase.md",
      "requiredReferences": [
        "cli-runtime",
        "artifact-contract",
        "ui-brand"
      ],
      "requiredTemplates": [
        "summary"
      ]
    },
    {
      "name": "kata-verify-work",
      "description": "Use when the user asks to verify, validate, review, demo, or sign off on Kata work.",
      "workflow": "verify-work",
      "contractOperations": [
        "project.getContext",
        "task.list",
        "task.updateStatus",
        "artifact.list",
        "artifact.read",
        "artifact.write"
      ],
      "setupHint": "If Kata CLI is not configured, run `npx @kata-sh/cli setup`, then verify with `npx @kata-sh/cli doctor`.",
      "legacyCommand": "apps/orchestrator-legacy/commands/kata/verify-work.md",
      "legacyWorkflow": "apps/orchestrator-legacy/kata/workflows/verify-work.md",
      "requiredReferences": [
        "cli-runtime",
        "artifact-contract",
        "ui-brand"
      ],
      "requiredTemplates": [
        "UAT",
        "verification-report"
      ]
    },
    {
      "name": "kata-complete-milestone",
      "description": "Use when the user wants to finish, close, ship, archive, or mark the active Kata milestone complete.",
      "workflow": "complete-milestone",
      "contractOperations": [
        "milestone.getActive",
        "milestone.complete",
        "artifact.write"
      ],
      "setupHint": "If Kata CLI is not configured, run `npx @kata-sh/cli setup`, then verify with `npx @kata-sh/cli doctor`.",
      "legacyCommand": "apps/orchestrator-legacy/commands/kata/complete-milestone.md",
      "legacyWorkflow": "apps/orchestrator-legacy/kata/workflows/complete-milestone.md",
      "requiredReferences": [
        "cli-runtime",
        "artifact-contract",
        "ui-brand"
      ],
      "requiredTemplates": [
        "milestone-archive",
        "retrospective"
      ]
    },
    {
      "name": "kata-progress",
      "description": "Use when the user asks for Kata project status, milestone progress, task progress, or execution state.",
      "workflow": "progress",
      "contractOperations": [
        "project.getContext",
        "milestone.getActive",
        "slice.list",
        "task.list",
        "artifact.list",
        "execution.getStatus"
      ],
      "setupHint": "If Kata CLI is not configured, run `npx @kata-sh/cli setup`, then verify with `npx @kata-sh/cli doctor`.",
      "legacyCommand": "apps/orchestrator-legacy/commands/kata/progress.md",
      "legacyWorkflow": "apps/orchestrator-legacy/kata/workflows/progress.md",
      "requiredReferences": [
        "cli-runtime",
        "artifact-contract"
      ],
      "requiredTemplates": []
    },
    {
      "name": "kata-health",
      "description": "Use when the user asks whether Kata is configured correctly, healthy, connected, or ready to run.",
      "workflow": "health",
      "contractOperations": [
        "health.check",
        "project.getContext"
      ],
      "setupHint": "If Kata CLI is not configured, run `npx @kata-sh/cli setup`, then verify with `npx @kata-sh/cli doctor`.",
      "legacyCommand": "apps/orchestrator-legacy/commands/kata/health.md",
      "legacyWorkflow": "apps/orchestrator-legacy/kata/workflows/health.md",
      "requiredReferences": [
        "cli-runtime",
        "artifact-contract"
      ],
      "requiredTemplates": []
    }
  ]
}
