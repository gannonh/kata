Complete slice {{sliceId}} ("{{sliceTitle}}") of milestone {{milestoneId}}. All tasks are done. Read the templates at `~/.kata-cli/agent/extensions/kata/templates/slice-summary.md` and `uat.md`. If a `Kata Skill Preferences` block is present in system context, use it to decide which skills to load and follow during completion, without relaxing required verification or artifact rules. Write the slice summary (`{{sliceId}}-SUMMARY`) and UAT (`{{sliceId}}-UAT`) to Linear using `kata_write_document` scoped to the slice issue. Fill the `UAT Type` plus `Not Proven By This UAT` sections explicitly so the artifact states what class of acceptance it covers and what still remains unproven. Review task summaries for `key_decisions` and ensure significant decisions are appended to `DECISIONS` via `kata_read_document("DECISIONS")` + `kata_write_document("DECISIONS", ...)`. Mark the slice checkbox done in `{{milestoneId}}-ROADMAP`, update `{{milestoneId}}-SUMMARY`, and leave the slice branch clean for the extension to continue lifecycle actions.
