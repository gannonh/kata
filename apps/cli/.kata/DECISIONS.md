# Decisions Register

<!-- Append-only. Never edit or remove existing rows.
     To reverse a decision, add a new row that supersedes it.
     Read this file at the start of any planning or research phase. -->

| # | When | Scope | Decision | Choice | Rationale | Revisable? |
|---|------|-------|----------|--------|-----------|------------|
| D001 | M001 | integration | MCP delivery model | Auto-seed pi-mcp-adapter in settings.json packages | Users get MCP out of the box; pi's package manager auto-installs on startup | Yes — if bundling source directly is preferred later |
| D002 | M001 | config | MCP config path | `~/.kata-cli/agent/mcp.json` via `--mcp-config` injection | Kata's config dir is `~/.kata-cli/`; adapter defaults to `~/.pi/agent/` which is wrong for Kata users | No — must stay in Kata's config dir |
| D003 | M001 | convention | mcp.json scaffold strategy | Create only if absent; never overwrite | Preserve user's MCP server config across Kata updates | No |
| D004 | M002 | arch | Workflow mode model | Switchable file mode vs Linear mode per-project | Teams using Linear don't want parallel local files; teams without Linear keep file mode | No |
| D005 | M002 | arch | Linear entity mapping | Project→Project, Milestone→Milestone, Slice→Parent Issue, Task→Sub-Issue | Uses Linear's native sub-issue hierarchy; slices as parent issues give natural grouping | No |
| D006 | M002 | arch | Artifact storage | Linear Documents attached to projects/issues | Markdown-native, searchable in Linear UI, no separate storage system | No |
| D007 | M002 | arch | Linear client approach | Port GraphQL client from schpet/linear-cli to Node TS extension | Native integration, no MCP/OAuth/Deno dependency for Kata operations | No |
| D008 | M002 | integration | Linear auth | Personal API key via secure_env_collect | Simpler than OAuth, works headlessly for agents, no browser flow | Yes — if team OAuth is needed later |
| D009 | M002 | arch | State derivation in Linear mode | Derive from Linear API queries, no local state files | Linear is single source of truth; no sync/cache complexity | No |
| D010 | M002 | arch | Workflow prompt strategy | Separate LINEAR-WORKFLOW.md prompt | Clean separation from file-mode prompt; no complex conditionals in one document | No |
| D011 | M003 | arch | PR lifecycle as separate milestone | M003 independent of M002 | PR lifecycle is mode-agnostic (works in file-mode and Linear-mode); Linear cross-linking is additive in S06 | No |
| D012 | M003 | arch | PR granularity | One PR per slice | Slices are demoable vertical increments with their own branch; tasks are too granular, milestones too coarse | No |
| D013 | M003 | arch | PR reviewer implementation | Bundled custom subagents, not skill-based role-play | Kata already ships 3 custom subagents; each reviewer gets isolated context with focused prompt; same resource-loader sync pattern | No |
| D014 | M003 | arch | Slice completion → PR → merge flow | Tasks done → PR created → merge is separate action → merge confirms completion | PR creation and merge are decoupled; human or agent can review between creation and merge | No |
| D015 | M003 | integration | GitHub CLI dependency | All GitHub operations via `gh` CLI | Proven in user's existing skills; avoids building a GitHub API client; `gh` handles auth, pagination, rate limiting | Yes — if native GitHub API is needed later |
| D016 | M003 | arch | PR body creation | File-backed via `create_pr_safe.py` | Prevents shell interpolation corruption of markdown in PR bodies; proven pattern from user's existing tooling | No |
| D017 | M002/S02 | config | Linear mode config storage | Extend `.kata/preferences.md` with `workflow` + `linear` sections; keep legacy `.kata/PREFERENCES.md` as read-only fallback | Reuses Kata’s existing per-project config surface, avoids a second config file, and fixes the current lowercase/uppercase split without breaking existing projects | Yes — if Linear config outgrows preferences later |
| D018 | M002/S02 | arch | Mode-switch branching seam | Centralize mode/config resolution in `linear-config.ts` and make commands/entrypoints consume it instead of parsing preferences ad hoc | Gives later slices one stable seam for prompt swapping, state derivation, and auto-mode routing while keeping file mode unchanged | No |
| D019 | M002/S02 | observability | Workflow config inspection surface | Make `/kata prefs status` the canonical user/agent surface for active mode and Linear config health | Future agents can inspect one stable command instead of parsing raw preference files, and users get actionable redacted diagnostics for missing auth or invalid bindings | No |
| D020 | M002/S02/T04 | arch | Mode-aware entrypoint gating | Route workflow-sensitive entrypoints through `linear-config.ts` guards before reading file-backed Kata state or prompts | Prevents Linear-configured projects from silently drifting into `.kata` file mode and gives S05/S06 one seam for status/prompt swapping | No |
| D021 | M002/S03 | convention | Kata entity title format | `[M001] Title` bracket prefix for all Linear entity names/titles | Visually distinct in Linear UI, regex-parseable (`/^\[([A-Z]\d+)\] (.+)$/`), round-trips cleanly; avoids colon prefix which conflicts with Linear's own identifier format | No |
| D022 | M002/S03 | convention | Kata label scheme | Three flat labels: `kata:milestone` (provisioned only), `kata:slice` (applied to slice issues), `kata:task` (applied to task sub-issues); fixed colors | Labels can only be applied to issues; LinearMilestones don't support labels so `kata:milestone` is provisioned for future use. Flat labels (not group hierarchy) are simpler and sufficient for S05 filtering | Yes — group hierarchy if Linear ever supports milestone-level labels |
| D023 | M002/S03 | convention | Kata phase → Linear state type mapping | `backlog`→`backlog`, `planning`→`unstarted`, `executing`→`started`, `verifying`→`started`, `done`→`completed`; `canceled`→`done` in reverse | Linear guarantees these five state types across all workspaces; `verifying` maps to `started` (same as executing) and is distinguished by sub-issue completion ratio in S05 state derivation | Yes — if a dedicated `verifying` state type is needed later |
| D024 | M002/S03 | arch | Entity functions module location | `linear-entities.ts` lives in the linear extension (`src/resources/extensions/linear/`), not the kata extension | Entity functions take explicit `client` + `config` args rather than reading preferences; keeps the linear extension self-contained and testable without kata extension imports | No |
| D025 | M002/S03 | arch | LinearEntityClient interface | Export a named `LinearEntityClient` structural interface from `linear-entities.ts` rather than using `Pick<LinearClient, ...>` inline | Named interface is reusable as a public contract, avoids importing the full `LinearClient` class (and its `http.ts` dependency) into the pure mapping module, and enables lightweight inline mocks in tests without extending the concrete class | No |
| D026 | M002/S04 | convention | Document attachment exclusivity | Documents attach to project OR issue via `DocumentAttachment = { projectId: string } \| { issueId: string }` discriminated union; milestone/root artifacts use `projectId`; slice/task artifacts use `issueId` | Linear-cli source confirms mutual exclusivity; setting both leads to undefined behavior. Discriminated union enforces this at the TypeScript type level — no runtime guard needed | No |
| D027 | M002/S04 | arch | Document upsert strategy | Title-scoped first-match upsert: `listDocuments({ projectId\|issueId, title })` → update first match if found, create if not; title collision by manual user creation updates the user's document | Mirrors `ensureLabel` pattern from S01; Linear's `DocumentFilter.title.eq` allows server-side title scoping without listing all documents; title collision is a known acceptable limitation | Yes — if Linear adds a dedicated upsert mutation |
| D028 | M002/S04 | integration | Linear document markdown normalization | Linear normalizes `- ` list bullets to `* ` and strips a single trailing newline on document write; canonical form for round-trip content is `* ` bullets and no trailing newline | Discovered in S04 integration tests; all downstream parsers in S05/S06 must handle `* ` bullets (not just `- `), and `- [ ]` checkbox patterns must also match `* [ ]` | No — this is Linear API behavior |
| D029 | M002/S05 | arch | LinearStateClient interface | Export a named `LinearStateClient` structural interface from `linear-state.ts` with two methods: `listMilestones(projectId)` and `listIssues(filter)` — the only surface `deriveLinearState` needs | Same pattern as D025 (LinearEntityClient) and D... (LinearDocumentClient); enables lightweight inline mocks in state derivation tests without importing the full LinearClient class | No |
| D030 | M002/S05 | arch | State derivation is pure-issue-state | `deriveLinearState` derives all state from Linear issue/milestone API queries only — no document content parsing for completion status | Avoids D028's bullet-normalization pitfall entirely; aligns with D009 (Linear as single source of truth); phase mapping uses Linear state types + `children.nodes` completion ratio, not checkbox parsing | No |
| D031 | M002/S05 | convention | verifying vs executing vs summarizing detection | In `deriveLinearState`: active slice with state type `started` → `executing` if 0 terminal children; `verifying` if some but not all children are terminal; `summarizing` if all children are terminal (and children.length > 0) | Extends D023's `started` → `executing`/`verifying` ambiguity; `summarizing` maps to the file-mode phase where all tasks are checked off but the slice is not yet completed — agent writes summary then advances slice to `completed` state | Yes — if Linear adds native verifying state type |
| D032 | M002/S05 | arch | `kata_derive_state` tool is zero-ceremony | The `kata_derive_state` tool reads `projectId`/`teamId` from project preferences and resolves `sliceLabelId` via `ensureKataLabels` internally — no params required from the caller | Maximizes agent ergonomics: one tool call gives full KataState without requiring prior label resolution or config lookup; consistent with how auto-mode will use it in S06 | Yes — if parameterized override is needed later |
| D033 | M002/S06 | arch | Linear-mode state derivation in auto.ts | `auto.ts` exports no `deriveKataState` import from `commands.ts`; instead `linear-auto.ts` provides `resolveLinearKataState()` which mirrors the same pattern (LinearClient + ensureKataLabels + deriveLinearState + blocked-on-error) | `commands.ts` imports from `auto.ts` so auto.ts importing from commands.ts creates a circular dependency; `linear-auto.ts` as a shared utility avoids the cycle while keeping the pattern consistent | No — circular dep constraint is structural |
| D034 | M002/S06 | arch | Workflow prompt injection via full content | `before_agent_start` reads `LINEAR-WORKFLOW.md` content from `protocol.path` and injects the full text into the system prompt when `protocol.ready` is true — no reference/lazy loading | Same approach used for KATA-WORKFLOW.md in guided prompts; full content at session start ensures agent has the protocol without needing a tool call to fetch it; acceptable given the ≤500 line target | Yes — could switch to lazy injection if token budget becomes a concern |
| D035 | M002/S06 | convention | `verifying` phase dispatches to execute-task in Linear auto | In `selectLinearPrompt`, `verifying` maps to the same prompt builder as `executing` (buildLinearExecuteTaskPrompt) — not a stop or UAT pause | D031 establishes verifying = some but not all tasks terminal; the right response is to continue executing remaining tasks, not pause for UAT; consistent with the research constraint "do not stop or ask for UAT in Linear mode for verifying phase" | Yes — if a dedicated Linear UAT phase is introduced later |
| D036 | M003/S01 | testing | pr-lifecycle test placement | Tests for pr-lifecycle modules (`pr-preferences.test.mjs`, `pr-body-composer.test.ts`) live in `src/resources/extensions/kata/tests/` | The `npm test` glob already covers `kata/tests/*.test.ts` and `*.test.mjs`; composer tests import `kata/paths.ts` + `kata/files.ts`, making kata/tests a natural home; avoids updating the test glob for one slice | Yes — if pr-lifecycle grows enough tests to warrant its own directory |
| D037 | M003/S01 | arch | `kata_create_pr` returns structured result | Tool handler returns `{ ok: boolean, phase?: string, error?: string, hint?: string, url?: string }` — never throws | Structured result lets agent branch on `ok` and inspect `phase` without prose parsing; `phase` enum covers all pre-flight check names; consistent with how Linear tools expose errors | No |
| D038 | M003/S01 | arch | Shell escaping for script invocation | `create_pr_safe.py` is invoked via single-quote-wrapped shellEscape helper rather than passing args as array to execSync | `create_pr_safe.py` exposes a CLI interface expecting a single command string; the script path and temp body file path may contain spaces; single-quote wrapping is the safe portable idiom for paths; avoids building a GitHub API client from scratch | No |
