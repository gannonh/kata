# Setup and Health

Run `npx @kata-sh/cli setup` for generic harness detection, or `npx @kata-sh/cli setup --pi` for Pi harnesses, then verify with `npx @kata-sh/cli doctor`.

Validate runtime health:

- `npx @kata-sh/cli doctor`
