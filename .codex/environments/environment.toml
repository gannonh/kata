# THIS IS AUTOGENERATED. DO NOT EDIT MANUALLY
version = 1
name = "kata-mono"

[setup]
script = ""

[setup.darwin]
script = '''
cp "$CODEX_SOURCE_TREE_PATH"/.env "$CODEX_WORKTREE_PATH"/.env
cp "$CODEX_SOURCE_TREE_PATH"/apps/desktop/.env.development "$CODEX_WORKTREE_PATH"/apps/desktop/.env.development
cp "$CODEX_SOURCE_TREE_PATH"/apps/symphony/WORKFLOW-desktop-github.md "$CODEX_WORKTREE_PATH"/apps/symphony/WORKFLOW-desktop-github.md
cp "$CODEX_SOURCE_TREE_PATH"/apps/symphony/WORKFLOW-desktop.md "$CODEX_WORKTREE_PATH"/apps/symphony/WORKFLOW-desktop.md

cd "$CODEX_WORKTREE_PATH" && pnpm install

cd "$CODEX_WORKTREE_PATH"/apps/symphony && cargo build --release
cd "$CODEX_WORKTREE_PATH" && pnpm build:cli:pi
'''
