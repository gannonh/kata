---
wave: 3
depends_on:
  - 01-01
  - 01-02
  - 01-03
  - 01-04
files_modified:
  - skills/adding-issues/SKILL.md
  - skills/checking-issues/SKILL.md
autonomous: true
---

<objective>
Add friendly deprecation handling so old todo command triggers show "did you mean /kata:check-issues?" messages.

**Why:** Context decision specifies deprecation warnings for old commands.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add deprecation triggers to adding-issues</name>
  <files>skills/adding-issues/SKILL.md</files>
  <action>
    Update the skill description to include old vocabulary triggers with deprecation note:

    In the frontmatter description, add triggers like:
    - "add todo" (deprecated)
    - "capture todo" (deprecated)
    - "new todo" (deprecated)

    Add a step at the start that detects if the invocation used deprecated vocabulary:

    ```markdown
    ## Step 0.5: Deprecation Notice

    If the user invoked with "todo" vocabulary, display:

    > **Note:** "todos" is now "issues". Using `/kata:add-issue`.

    Then proceed with the action (non-blocking).
    ```

    The deprecation is friendly - it still works, just informs the user.
  </action>
  <verify>grep -c "deprecated\|did you mean" skills/adding-issues/SKILL.md</verify>
  <done>Deprecation notice added for old vocabulary</done>
</task>

<task type="auto">
  <name>Task 2: Add deprecation triggers to checking-issues</name>
  <files>skills/checking-issues/SKILL.md</files>
  <action>
    Same pattern as adding-issues:

    Add old vocabulary triggers:
    - "check todos" (deprecated)
    - "list todos" (deprecated)
    - "pending todos" (deprecated)

    Add deprecation notice step that shows:

    > **Note:** "todos" is now "issues". Using `/kata:check-issues`.

    Non-blocking - workflow continues after message.
  </action>
  <verify>grep -c "deprecated\|did you mean" skills/checking-issues/SKILL.md</verify>
  <done>Deprecation notice added for old vocabulary</done>
</task>

</tasks>

<must_haves>
- [ ] Old trigger phrases ("add todo", "check todos") still work
- [ ] User sees helpful deprecation message pointing to new commands
- [ ] Workflow continues without blocking
</must_haves>

<verification>
```bash
# Check deprecation text exists
grep -i "deprecated\|did you mean\|now.*issues" skills/adding-issues/SKILL.md
grep -i "deprecated\|did you mean\|now.*issues" skills/checking-issues/SKILL.md
```
</verification>
